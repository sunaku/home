#!/bin/sh
# Undo apt-get installation history.
for log in $(ls -tr /var/log/apt/history.log*)
do
  case "$log" in
    (*.gz) zcat "$log" ;;
    (*)     cat "$log" ;;
  esac
done | sed -n '
  /^Install:/{
    s/^Install:/\nsudo apt-get remove/
    s/([^)]*)//g
    s/, //g
    p
  }
'
