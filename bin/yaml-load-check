#!/usr/bin/env ruby
#
# Loads the given YAML document and reports any errors encountered.
# If no errors were encountered, it prints "Syntax OK" to STDOUT.
#
# Written in 2011 by Suraj N. Kurapati <https://github.com/sunaku>

require 'yaml'
YAML.load(ARGF)
puts 'Syntax OK'
