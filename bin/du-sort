#!/bin/sh
#
# Sorts du(1) output, especially `du -h` output, in ascending order.
#
# Usage: du | du-sort
# Usage: du-sort < FILE
#
input="$(sort -n "$@")"
for unit in '\s' '[Kk]' '[Mm]' '[Gg]' '[Tt]' '[Pp]' '[Ee]' '[Zz]' '[Yy]'; do
  echo "$input" | grep "^[0-9\.]\+$unit"
done
