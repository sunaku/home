#!/bin/sh
#
# Usage: keyboard [LAYOUT]
#
# Changes the keyboard layout to the given value (such as "us" or "dvorak").
#
# Written in 2014 by Suraj N. Kurapati <https://github.com/sunaku>

if [ -n "$DISPLAY" ]; then
  setxkbmap -layout "$@"
else
  sudo loadkeys "$1"
fi
